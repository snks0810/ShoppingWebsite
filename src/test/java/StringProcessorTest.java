import ToyProject.StringProcessor;
import org.junit.Test;
import org.junit.jupiter.api.DisplayName;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.assertSame;

public class StringProcessorTest {

    // another one of those lovely unit tests that says "Error these two aren't the same"
    // then when you inspect it "These two are identical" :)

    //@Test
    //@DisplayName("Testing the £ removal on doubles")
    //public void removingPoundDouble(){
    //    ArrayList<String> input = new ArrayList<>();
    //    String inputString = "£1.00";
    //    input.add(inputString);
    //    double output = StringProcessor.poundRemoval(input).get(0);
    //    double expected = 1.00;
    //    assertSame(expected,output,"Can extract doubles from the strings");
    //}
}
